# 2.3.22 本地修复

## GitHub 发布验证（2026-09-16）

- 安装包内置 CLI 更新至 0.154.0，同时包含所有随程序提供的模型目录。
- 本机 Windows 桌面包 OpenAI.Codex 26.908.9136.0，其实际使用的 app-server 为 0.154.0-alpha.6.2：隔离虚拟 OAuth 网关路由测试、Astra 模型目录读取均通过；不更改真实登录，不发出付费请求。
- 发布包 CLI 0.154.0 同样通过以上两个测试。程序完整自检、隔离安装、安装包隐私扫描通过。
- 验证范围不等于完整桌面 UI 端到端登录测试，不保证未来未测试的桌面版本或第三方上游模型权限。
- GitHub 发布完成后，仅保留当前 Windows 版本包、稳定下载别名及其 SHA-256、更新清单；移除旧安装包下载，保留 Git 源码历史。macOS 本轮未构建，不沿用旧包冒充新版。

修复在旧会话中续聊时，近期用量未进入额度统计的问题。日期目录代表会话创建时间，不能用来判断是否有近期用量；扫描现在发现所有目录中的日志，再按文件更新时间筛选，继续使用增量缓存。

已存在日志会在刷新统计时重新发现，无需修改聊天记录或账号归属。恢复的历史用量按原始发生时间进入相应的账号与模型统计。

回归验证覆盖新旧日期目录同时存在、旧会话切换至 Sol、新账号归属、旧用量排除、缓存重启与增量追加不重复计数。提供 tools/UsageHistoryAudit，用临时缓存核对真实日志而不改动运行中的管理器。

版本为 2.3.22，网关端口 8339。桌面快捷方式指向新版本；安装不强制关闭旧管理器或网关。

快捷方式维护补充：本地构建自检成功后，以及安装包升级时，同步同一数据目录的桌面、开始菜单、任务栏及隐式固定入口。保留显式启动参数、跳过其他数据目录及备用版本、防止旧构建降级新版入口，并通知 Explorer 刷新链接。安装包构建检查同步脚本已纳入压缩包。此补充修改发布/安装脚本，不更换运行中的程序或网关。

验证结果：发布脚本完整自检通过。使用 2026-09-13 的真实日志，在目标账号下找回 50 条 Sol 用量事件，6,883,112 Token（包含缓存输入），时间为 22:36:12 至 23:07:46。临时缓存首次重算约 19.4 秒，第二次刷新约 340 毫秒，历史结果完全一致。核验没有改写账号、聊天日志或运行中管理器的缓存。
